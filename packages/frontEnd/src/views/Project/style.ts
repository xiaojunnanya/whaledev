import styled from 'styled-components'

export const ProjectStyled = styled.div`
  .projectHeaders {
    width: 100%;
    position: relative;

    .project-name {
      position: absolute;
      line-height: 42px;
      margin-left: 16px;
      z-index: 999;
      cursor: pointer;
    }
  }
`
